{
  "name": "sound-scape-explorer",
  "scripts": {
    "install": "bin/install.sh",
    "dev": "concurrently 'bin/run-client.sh' 'bin/run-server.sh'"
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "version": "1.0.1"
}
