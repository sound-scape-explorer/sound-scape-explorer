{
  "name": "sound-scape-explorer",
  "scripts": {
    "install": "install.sh && yarn process",
    "dev": "concurrently 'bin/front-dev.sh' 'bin/back-dev.sh'",
    "process": "processing/run.sh sample",
    "start": "yarn dev"
  },
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "version": "2.1.4"
}
