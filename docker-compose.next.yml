version: "3.7"

services:
  processing:
    image: ghcr.io/sound-scape-explorer/processing:next
    volumes:
      - ./sample:/sample

  back:
    image: ghcr.io/sound-scape-explorer/back:next
    ports:
      - "8081:8081"
    volumes:
      - ./sample:/sample
    depends_on:
      - processing

  front:
    image: ghcr.io/sound-scape-explorer/front:next
    ports:
      - "8080:8080"
    depends_on:
      - back
